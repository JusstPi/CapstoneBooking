from django.urls import path
from . import views

app_name = 'BookingApp'

urlpatterns = [
    # path('', views.Booking.as_view(), name = 'add_booking'),
    # path('editBooking/<int:id>',views.EditBooking.as_view(), name = 'editBooking'),
]